# search-ground-truth — rebaseline log

Chromium-style discipline: the frozen sidecars (`*.search-gt.json`) change
only through a row here. Every row answers *what changed, why, and which
kind of change it was* — `bug-fix` (the old expectation was wrong),
`intentional` (engine behavior legitimately changed; cells rebaselined), or
`regenerated` (OCR-leg expectations are tool-regenerated by definition —
Vision output is not stable across OS versions and is never hand-edited).

Sidecar provenance: `search-queries.json` (the T3.1 bank) is generated by
`tools/build_search_queries.py`; the frozen GT is emitted by
`tools/search_oracle.py freeze` from an official H3.1 run plus the
adjudication file for that session. Hand-editing a sidecar is a process
violation.

| # | date (session) | doc(s) | cells | kind | what / why |
|---|---|---|---|---|---|
| 1 | 2026-08-23 (P1.5) | packet · packet-scan-sim-150dpi | all (v1 creation) | initial | First freeze from the first official harness-sim H3.1 run at baseline `8bbf5c8c`, adjudicated per `adjudication-p15.json`: r10/r12 frozen to the product safety-rejection contract (0 hits; F12-07 carries the over-conservative-gate finding), burst cells (>100 hits/page) frozen from the higher-yield of the two runs (bufferingNewest drop, F12-08), OCR leg regenerated from run 1. |
| 2 | 2026-09-17 (S4-S2) | packet-scan-sim-150dpi | 126 cells: m04 · q11 · q12 · q13 · q15 · q16 · q17 · q18 · q19 · r01 · r04 · r11 (gains only, +366 hits, 0 lost) | regenerated | The OCR text normalizer keeps digit runs intact on labelled value lines (the digit-run rule, landed 2026-09-17 in the product): the phones, the card number, the dates and the SSN whose `05` had read as `OS` now match; the OCR leg regenerates from run 1 of the post-rule run by definition. |
| 3 | 2026-09-17 (S4-S2) | packet-scan-sim-150dpi | 16 cells: r06 `\d+` at every OCR-on vector (+747 hits, 0 lost) | bug-fix | The result stream no longer drops hits on pages with more than a hundred matches (`bufferingNewest(100)` → unbounded, the result cap as the bound): the two dense pages deliver their full counts and run 1 equals run 2, so the max-of-runs reconciliation of row 1 is no longer exercised (`burst_max_cells` empty). The packet's r06 cells were unchanged by the re-freeze — the 2026-08-23 packet freeze had caught full runs on the text leg — although the same drop was measured there run-to-run on 2026-09-17 (605 vs 526 on one cell of the same tree). |
| 4 | 2026-09-17 (S4-S2) | packet · packet-scan-sim-150dpi | 24 cells: q24 (the U+2011 NON-BREAKING HYPHEN query) at every vector with `normalizeSmartPunctuation` on — 13 on the packet, 11 on scan-sim (+72 hits, 0 lost); the four fold-off vectors stay 0 | bug-fix | U+2010 HYPHEN joined the smart-punctuation fold: NFKC maps U+2011 to U+2010, so the query now folds on the Unicode path and matches exactly where its ASCII twin q08 does (previously only with `normalizeUnicode` off). `expect_packet_occurrences` in the bank stays 0 (a hand-authored field, not a frozen cell). |
| 5 | 2026-09-17 (S4-S2) | packet · packet-scan-sim-150dpi | 3 cells: m07 (`["Hartwell", ""]`) at the AND vectors — packet v09 0 → 28 and v10 0 → 14, scan-sim v09 0 → 27 (+69 hits, 0 lost); every OR cell unchanged | bug-fix | Empty and whitespace-only entries in a multi-term list are dropped at entry, so the conjunction no longer demands a term no page can carry; the AND cells now equal the single-term cells of q01 (`Hartwell`) at the same vectors. |
