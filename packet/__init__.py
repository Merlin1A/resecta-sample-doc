"""packet -- the Resecta sample/test document-packet generator.

A per-genre document FAMILY built on the deterministic reportlab + pypdf pipeline. The first member
is the Hartwell loan/mortgage application packet: a PII-dense synthetic document that ships as a
second in-app sample AND is the primary labeled test corpus.
"""
